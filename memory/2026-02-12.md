# 2026-02-12

## PackCrafter repo access + analysis
- Cloned private repo to `/root/.openclaw/workspace/packcrafter-ai` on branch `dev`.
- Current head when cloned: `0bdcbbf` ("Add premium badge to index page user dropdown").
- User asked for conceptual walkthrough before guiding next steps.

## Key technical understanding (to carry into next chat)
- App is a premium-gated AI Minecraft modpack generator.
- Main generation flow: candidate pool from Modrinth -> Claude slot selection -> dependency resolution/repair/pruning -> `.mrpack` build -> user-scoped download.
- Core backend entry points: `src/api/routes.ts` (`/api/generate`, `/api/download/:filename`, `/api/me`, `/api/health`).
- Pack generation core services:
  - `src/services/candidateBuilder.ts`
  - `src/services/llmSelector.ts`
  - `src/services/dependencyResolver.ts`
  - `src/services/packBuilder.ts`
- Billing/premium gating:
  - `src/api/billing.ts`
  - premium middleware on generate/download.
- Jobs tracked in Supabase via `src/services/jobService.ts` with ownership checks on artifact download.

## User intent
- User plans to continue in a new chat and wants this repo understanding preserved.
